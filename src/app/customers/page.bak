import { Customers } from "@/components/customers";
import { createPageMetadata } from "@/lib/metadata";

export const metadata = createPageMetadata({
  title: "DX Ecosystem — Tool Provider Directory",
  description:
    "Discover AI providers, extensions, and integrations for your DX toolchain. Connect models, host adapters, and build workflows.",
  path: "/customers",
  og: {
    title: "Ecosystem",
    description: "Discover providers, extensions, and integrations",
  },
});

export default function Page() {
  return <Customers />;
}
